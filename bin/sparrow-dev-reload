#!/bin/bash
# Description: Reload web browser if app is in development mode
# This will eventually be folded into a `sparrow dev` command

sparrow compose exec frontend \
  /app/node_modules/.bin/browser-sync reload
