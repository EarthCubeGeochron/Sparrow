#!/usr/bin/env zsh

indb_conn="$(sparrow config database)"
outdb='sparrow_test'
outdb_conn="postgresql:///$outdb"
createdb "$outdb"
SPARROW_DATABASE="$outdb_conn" sparrow init

migra "$indb_conn" "$outdb_conn" \
  --unsafe > schema-migration.sql
dropdb "$outdb"
