#!/bin/bash

cd "$SPARROW_PATH"

docker-compose exec db \
  pg_dump -C -Upostgres sparrow > $1


#ssh steno 'bunzip2 | sudo docker load'
#docker run --rm -v dist_pgdata:/from alpine ash -c "cd /from ; tar -cf - . " \
#| ssh steno 'sudo docker run --rm -i -v dist_pgdata:/to alpine ash -c "cd /to ; tar -xpvf - " '
#ssh -T steno <<'EOSSH'
#cd wiscar-labdata
#sudo docker-compose down
#sudo docker load < dist_web.bz
#sudo docker create -v pgdata:/data --name DATA busybox true
#sudo docker run --rm --volumes-from DATA -v $(pwd):/backup busybox tar xvf /backup/data.bz2 data/
#EOSSH

#docker run --volumes-from labdata-database busybox tar -cO /data | gzip -c > volume.tgz
