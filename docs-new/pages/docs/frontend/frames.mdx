import { DocsPage } from "components";

# Frames

Frames are what allow users to customize their sparrow frontend through the use of [frontend plugins](/docs/getting-started/plugins).
Frames exist at different places on the user interface and be accessed via their unqiue ID. 

Frames:

- `pageFooter`
- `samplePage`

export default DocsPage;
