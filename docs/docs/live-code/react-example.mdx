---
id: react-example
title: React Component Example
sidebar_label: React Component
---

import {testJson, ageJson} from '../../static/testdata.js
'
```jsx live
export function SampleCardExample() {
  const [age, setAge] = useState(false);
  console.log(age);
    const sampleURL = "https://sparrow-data.org/labs/wiscar/api/v1/sample?id=20";
    const ageURL =
    "https://sparrow-data.org/labs/wiscar/api/v1/age_context?sample_id=20";
    const data = age ? ageJson : testJson;
    const url = age ? ageURL : sampleURL;
    const AgeContext = (data) => {
    const {
        data: { value, error },
    } = data;
return (
<div>
  {"Age: " +
    Number(value).toFixed(3) +
    " ± " +
    Number(error).toFixed(3) +
    " Ma"}
</div>
);
};
return (
<div>
<div
style={{
          display: "flex",
          justifyContent: "center",
          paddingTop: "300px",
        }} >
<div>
<div>{"API route that data is being fetched from: " + url}</div>
          <div
            style={{
              display: "flex",
              flexDirection: "column",
              alignItems: "flex-start",
            }}
          >
            <button onClick={() => setAge(!age)}>Fetch the Age!</button>
          </div>
          <div style={{ border: "5px solid black" }}>
            <div
              style={{
                padding: "10px",
                display: "flex",
                flexDirection: "column",
                flexWrap: "wrap",
              }}
            >
              <div style={{ padding: "5px" }}>{data.name}</div>
              <div style={{ padding: "5px" }}>{data.material}</div>
              <div
                style={{ padding: "5px", alignSelf: "flex-end" }}
              >{`Latitude: ${data.geometry.coordinates[1]}`}</div>
              <div
                style={{ padding: "5px", alignSelf: "flex-end" }}
              >{`Longitude: ${data.geometry.coordinates[0]}`}</div>
              {age ? (
                <div style={{ padding: "5px", alignSelf: "flex-end" }}>
                  <AgeContext data={data} />
                </div>
              ) : null}
            </div>
          </div>
        </div>
      </div>
    </div>
);
}
```
```

```
