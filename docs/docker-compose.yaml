version: "3.4"
services:
  frontend:
    build:
      context: ..
      dockerfile: docs/Dockerfile
    expose:
      - 8000
    ports:
      - "8000:8000"
    volumes:
      - .:/docs
      - /docs/.cache
      - /docs/node_modules
      - public:/docs/public
volumes:
  public:
