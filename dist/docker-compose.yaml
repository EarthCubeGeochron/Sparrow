version: "3"
services:
  web:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - postgis
  postgis:
    image: geographica/postgis:spry_sparrow
    ports:
      - "54320:5432"
    expose:
      - 5432
    volumes:
      - db-data:/data
volumes:
  db-data:
