"""
This is a configuration file that sets configuration
for the backend. It's location is settable with the
`SPARROW_BACKEND_CONFIG` environment variable. For dockerized
applications, the values should be the same everywhere.
"""
DATABASE="postgresql://postgres@db:5432/sparrow"
ASSETS_DIRECTORY="/assets"
SPARROW_COMMANDS = "/sparrow-commands"
